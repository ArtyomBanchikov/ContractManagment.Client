<UserControl x:Class="ContractManagment.Client.MVVM.View.RecordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContractManagment.Client.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ListView Background="#646770"
                  ItemsSource="{Binding FullRecords}"
                  SelectedItem="{Binding SelectedFullRecord}"
                  BorderThickness="0"
                  Grid.Row="0"
                  ItemContainerStyle="{StaticResource RecordCardTheme}"/>
        <Button Content="Открыть"
                Grid.Row="1"
                Style="{StaticResource AddButtonTheme}"
                Command="{Binding RecordOpenCommand}"/>
    </Grid>
</UserControl>
